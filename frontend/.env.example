# Vite environment variables for the frontend
# Prefix custom variables with VITE_ to expose them to the client.

# URL of the backend API (include protocol and port if non-default)
VITE_API_URL=http://localhost:8000

# Optional: increase timeout for long-running backend ops (ms)
VITE_API_TIMEOUT=600000
